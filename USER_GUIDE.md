# Heading 1
## Heading 2
`Reference or line code`
```
  Full Program
  Goes in
  This area
```
[Name of Website]URL
## Introduction — what the program does, in plain English.
   This program receives a student's grading scheme, takes the score and the name of assignments, and performs calculations to show the student his final results.
   
3. Getting Started — how to launch the program (one or two sentences referencing the README's build instructions).
   In order to launch this program, the user must navigate to and click the "code" button within the home page of the repository. This will redirect the user to Codespaces. Next, the user should click on the "Run and Debug" button located on the lefthand side menu. If the user has not done so already, he will be prompted to install the necessary packets. The necessary requirements to launch the program can be reviewed in the README.md file's "How to Run", "How to Launch", and "Requirements" sections.
5. Step-by-Step Walkthrough — number every prompt the user will see, in order, with: what the prompt is asking for, the data type / format expected (e.g., "a whole number", "a decimal number such as 87.5"), and acceptable input ranges or constraints (e.g., "Total Points Possible must be a positive integer").
6. Sentinel Value Explanation — explain that entering a negative number for an assignment score ends input and triggers the calculation.
7. Sample Session — paste a complete example run (input + output) so the user can see what success looks like.
8. Understanding Your Results — explain what each line of the final output means (Total Points Earned, Total Percentage, Final Letter Grade).
9. Troubleshooting — at least 2 common mistakes (e.g., entering letters where a number is expected, forgetting that the sentinel is negative).
